# TODO
- [ ] Update `src/app/layout.tsx` to include browser tab/app icons (favicon) links using the SVG in `public/`.
- [x] Ensure Next.js metadata `icons` points to `public/favicon.svg`.
- [ ] (Later) Verify by running dev server and checking browser tab icon + cache refresh.


